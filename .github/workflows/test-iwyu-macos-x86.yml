name: iwyu-macos-x86

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  test-iwyu-macos-x86:
    name: IWYU (macOS x86_64)
    runs-on: macos-15-intel

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install uv
      run: curl -LsSf https://astral.sh/uv/install.sh | sh

    - name: Install package
      run: |
        uv venv
        uv pip install -e ".[dev]"

    - name: Display platform information
      run: |
        echo "Platform: macOS x86_64 - IWYU"
        uname -a
        python --version

    - name: Install z3 dependency
      run: |
        brew install z3
        echo "z3 installed for libLLVM.dylib dependency"

    - name: Download and install toolchain
      run: |
        uv run clang-tool-chain-c --version
      timeout-minutes: 20

    - name: Purge old IWYU installation (force re-download with new archives)
      run: |
        rm -rf ~/.clang-tool-chain/iwyu
        echo "Purged old IWYU installation to force fresh download"
      shell: bash

    - name: Run IWYU tests
      run: |
        uv run pytest tests/test_iwyu.py -v
